{% extends 'base.html' %}
{% load static %}
{% block title %}Message Templates - {{ organization.name }}{% endblock %}
{% block content %}
<div class="container py-4">
    <h1>Message Templates for {{ organization.name }}</h1>
    {% if error %}
        <div class="alert alert-danger">{{ error }}</div>
    {% endif %}

    <!-- Create Custom Template Form -->
    <div class="card mb-4">
        <div class="card-header">
            <h5 class="mb-0">Create Custom Template</h5>
        </div>
        <div class="card-body">
            <form method="post">
                {% csrf_token %}
                <div class="mb-3">
                    <label class="form-label">Template Name</label>
                    <input type="text" name="name" class="form-control" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Content</label>
                    <textarea name="content" class="form-control" rows="3" required></textarea>
                </div>
                <button type="submit" class="btn btn-primary">Create Template</button>
            </form>
        </div>
    </div>

    <!-- Pre-built Templates -->
    {% if pre_built_templates %}
    <div class="card mb-4">
        <div class="card-header bg-info text-white">
            <h5 class="mb-0">Pre-built Templates</h5>
            <small>Ready-to-use templates across different categories</small>
        </div>
        <div class="card-body">
            <div class="row">
                {% for template in pre_built_templates %}
                <div class="col-md-6 col-lg-4 mb-3">
                    <div class="card h-100">
                        <div class="card-body">
                            <h6 class="card-title">{{ template.name }}</h6>
                            <p class="card-text small">{{ template.content|truncatechars:100 }}</p>
                            <button class="btn btn-sm btn-outline-primary" onclick="useTemplate('{{ template.content|escapejs }}')">Use This Template</button>
                        </div>
                    </div>
                </div>
                {% endfor %}
            </div>
        </div>
    </div>
    {% endif %}

    <!-- Custom Templates -->
    <div class="card">
        <div class="card-header">
            <h5 class="mb-0">Your Custom Templates</h5>
        </div>
        <div class="card-body">
            {% if custom_templates %}
            <div class="list-group">
                {% for t in custom_templates %}
                    <li class="list-group-item d-flex justify-content-between align-items-start">
                        <div>
                            <strong>{{ t.name }}</strong> — {{ t.content }} <br>
                            <small class="text-muted">Created: {{ t.created_at|date:'Y-m-d H:i' }}</small>
                        </div>
                        <div>
                            <a href="{% url 'org_template_edit' organization.slug t.id %}" class="btn btn-sm btn-outline-primary me-2">Edit</a>
                            <a href="{% url 'org_template_delete' organization.slug t.id %}" class="btn btn-sm btn-outline-danger">Delete</a>
                        </div>
                    </li>
                {% endfor %}
            </div>
            {% else %}
            <p class="text-muted">No custom templates yet. Create your first template above!</p>
            {% endif %}
        </div>
    </div>

    <div class="mt-3">
        <a href="{% url 'org_dashboard' organization.slug %}" class="btn btn-secondary">Back to Dashboard</a>
    </div>
</div>

<script>
function useTemplate(content) {
    // Copy template content to the create form
    document.querySelector('textarea[name="content"]').value = content;
    // Scroll to the form
    document.querySelector('.card-header h5').scrollIntoView({ behavior: 'smooth' });
    // Focus on the name field
    document.querySelector('input[name="name"]').focus();
}
</script>
{% endblock %}
